import { OtpProps } from "lib/types";

export const OtpCard = (props: OtpProps) => {
    return (
        <div>
            <div>props.name</div>
            <div>

            </div>
        </div>
    )
}
